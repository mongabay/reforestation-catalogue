export interface ProjectCatalogProps {
  /** Open the projects in a new window. Default to `false`. */
  openInNewWindow?: boolean;
  /** Highlight the sorting category on the charts. Default to `true`. */
  hightlightSortingCategory?: boolean;
}
